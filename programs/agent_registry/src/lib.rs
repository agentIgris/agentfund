use anchor_lang::prelude::*;

// Placeholder program id — replaced with the value of REGISTRY_PROGRAM_ID
// (env) once deployed. Keep in sync with Anchor.toml.
declare_id!("AgReg11111111111111111111111111111111111");

#[program]
pub mod agent_registry {
    use super::*;

    // Instructions (register_agent, create_project, update_project_status)
    // and account structs (AgentAccount, ProjectAccount) are implemented
    // by the program builder per implementation_plan.md.
}
