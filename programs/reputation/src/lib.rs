use anchor_lang::prelude::*;

// Placeholder program id — replaced with the value of REPUTATION_PROGRAM_ID
// (env) once deployed. Keep in sync with Anchor.toml.
declare_id!("Reput1111111111111111111111111111111111111");

#[program]
pub mod reputation {
    use super::*;

    // Instructions (mint_reputation, update_reputation, get_reputation)
    // using Metaplex Bubblegum compressed NFTs are implemented by the
    // program builder per implementation_plan.md.
}
